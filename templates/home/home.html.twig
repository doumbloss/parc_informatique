<!DOCTYPE html>
<html lang="fr" data-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>UGANC - Gestion du Parc Informatique</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.10.0/font/bootstrap-icons.min.css" rel="stylesheet">
    <style>
        :root {
            --primary-color: #2563eb;
            --primary-dark: #1d4ed8;
            --secondary-color: #7c3aed;
            --accent-color: #f59e0b;
            --content-bg: #f8fafc;
            --sidebar-bg: #1e293b;
            --text-primary: #1f2937;
            --text-secondary: #6b7280;
            --border-color: #e5e7eb;
            --shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
            --shadow-lg: 0 20px 25px -5px rgba(0, 0, 0, 0.15);
            --transition: all 0.3s ease;
        }

        [data-theme="dark"] {
            --primary-color: #3b82f6;
            --content-bg: #0f172a;
            --sidebar-bg: #1e293b;
            --text-primary: #f1f5f9;
            --text-secondary: #94a3b8;
            --border-color: #334155;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            background: var(--content-bg);
            color: var(--text-primary);
            line-height: 1.6;
        }

        /* Hero Section avec particules animées */
        .hero-section {
            background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
            color: white;
            min-height: 100vh;
            display: flex;
            align-items: center;
            position: relative;
            overflow: hidden;
        }

        .hero-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: 
                radial-gradient(circle at 20% 50%, rgba(255,255,255,0.1) 0%, transparent 50%),
                radial-gradient(circle at 80% 20%, rgba(255,255,255,0.08) 0%, transparent 50%),
                radial-gradient(circle at 40% 80%, rgba(255,255,255,0.06) 0%, transparent 50%);
            animation: float 20s ease-in-out infinite;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0px) rotate(0deg); }
            33% { transform: translateY(-20px) rotate(1deg); }
            66% { transform: translateY(10px) rotate(-1deg); }
        }

        .hero-content {
            position: relative;
            z-index: 2;
            max-width: 900px;
            text-align: center;
        }

        .hero-title {
            font-size: clamp(2.5rem, 5vw, 4rem);
            font-weight: 800;
            margin-bottom: 1.5rem;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
            background: linear-gradient(45deg, #ffffff, #e2e8f0);
            background-clip: text;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .hero-subtitle {
            font-size: 1.4rem;
            opacity: 0.95;
            margin-bottom: 3rem;
            font-weight: 300;
            line-height: 1.8;
        }

        .btn-start {
            background: rgba(255, 255, 255, 0.15);
            color: white;
            border: 2px solid rgba(255, 255, 255, 0.3);
            font-weight: 600;
            padding: 1rem 3rem;
            border-radius: 50px;
            text-transform: uppercase;
            font-size: 0.9rem;
            letter-spacing: 1px;
            transition: var(--transition);
            backdrop-filter: blur(10px);
            position: relative;
            overflow: hidden;
        }

        .btn-start::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
            transition: left 0.5s;
        }

        .btn-start:hover::before {
            left: 100%;
        }

        .btn-start:hover {
            background: white;
            color: var(--primary-color);
            transform: translateY(-3px);
            box-shadow: var(--shadow-lg);
        }

        /* Stats Section */
        .stats-section {
            background: white;
            padding: 3rem 0;
            margin-top: -2rem;
            position: relative;
            z-index: 3;
        }

        [data-theme="dark"] .stats-section {
            background: var(--sidebar-bg);
        }

        .stat-card {
            text-align: center;
            padding: 2rem 1rem;
        }

        .stat-number {
            font-size: 3rem;
            font-weight: 700;
            color: var(--primary-color);
            display: block;
        }

        .stat-label {
            color: var(--text-secondary);
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        /* Features Section */
        .features-section {
            padding: 6rem 0;
            background: var(--content-bg);
            position: relative;
        }

        .section-title {
            font-size: 2.5rem;
            font-weight: 700;
            text-align: center;
            margin-bottom: 1rem;
            color: var(--text-primary);
        }

        .section-subtitle {
            text-align: center;
            color: var(--text-secondary);
            font-size: 1.1rem;
            max-width: 600px;
            margin: 0 auto 4rem;
            line-height: 1.7;
        }

        .feature-card {
            background: white;
            border-radius: 20px;
            padding: 3rem 2rem;
            margin-bottom: 2rem;
            box-shadow: var(--shadow);
            transition: var(--transition);
            text-align: center;
            border: 1px solid var(--border-color);
            position: relative;
            overflow: hidden;
        }

        .feature-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, var(--primary-color), var(--secondary-color));
            transform: scaleX(0);
            transition: transform 0.3s ease;
        }

        .feature-card:hover::before {
            transform: scaleX(1);
        }

        [data-theme="dark"] .feature-card {
            background: var(--sidebar-bg);
            color: var(--text-primary);
            border-color: #334155;
        }

        .feature-card:hover {
            transform: translateY(-10px);
            box-shadow: var(--shadow-lg);
        }

        .feature-icon {
            font-size: 3rem;
            color: var(--primary-color);
            margin-bottom: 1.5rem;
            display: inline-block;
            padding: 1rem;
            background: rgba(37, 99, 235, 0.1);
            border-radius: 50%;
            transition: var(--transition);
        }

        .feature-card:hover .feature-icon {
            transform: scale(1.1);
            background: var(--primary-color);
            color: white;
        }

        .feature-title {
            font-size: 1.4rem;
            font-weight: 600;
            margin-bottom: 1rem;
            color: var(--text-primary);
        }

        .feature-description {
            color: var(--text-secondary);
            line-height: 1.7;
        }

        /* Image Section */
        .showcase-section {
            padding: 6rem 0;
            background: linear-gradient(135deg, #f7fafc, #edf2f7);
        }

        [data-theme="dark"] .showcase-section {
            background: linear-gradient(135deg, var(--content-bg), var(--sidebar-bg));
        }

        .showcase-item {
            margin-bottom: 5rem;
        }

        .showcase-image {
            width: 100%;
            height: 400px;
            border-radius: 20px;
            object-fit: cover;
            box-shadow: var(--shadow-lg);
            transition: var(--transition);
        }

        .showcase-image:hover {
            transform: scale(1.02);
        }

        .showcase-content h3 {
            font-size: 2rem;
            font-weight: 700;
            margin-bottom: 1.5rem;
            color: var(--text-primary);
        }

        .showcase-content p {
            font-size: 1.1rem;
            color: var(--text-secondary);
            line-height: 1.8;
        }

        /* Technology Section */
        .tech-section {
            padding: 6rem 0;
            background: white;
            text-align: center;
        }

        [data-theme="dark"] .tech-section {
            background: var(--sidebar-bg);
        }

        .tech-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 2rem;
            margin-top: 3rem;
        }

        .tech-item {
            padding: 2rem;
            border-radius: 15px;
            background: var(--content-bg);
            transition: var(--transition);
        }

        [data-theme="dark"] .tech-item {
            background: var(--content-bg);
        }

        .tech-item:hover {
            transform: translateY(-5px);
            box-shadow: var(--shadow);
        }

        .tech-icon {
            font-size: 2.5rem;
            color: var(--primary-color);
            margin-bottom: 1rem;
        }

        .tech-name {
            font-weight: 600;
            color: var(--text-primary);
        }

        /* Animations */
        .animate-on-scroll {
            opacity: 0;
            transform: translateY(30px);
            transition: opacity 0.8s ease, transform 0.8s ease;
        }

        .animate-on-scroll.visible {
            opacity: 1;
            transform: translateY(0);
        }

        /* Theme Toggle */
        .theme-toggle {
            position: fixed;
            top: 20px;
            right: 20px;
            z-index: 1000;
            background: rgba(255, 255, 255, 0.9);
            border: none;
            border-radius: 50%;
            width: 50px;
            height: 50px;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: var(--shadow);
            transition: var(--transition);
            backdrop-filter: blur(10px);
        }

        [data-theme="dark"] .theme-toggle {
            background: rgba(30, 41, 59, 0.9);
            color: white;
        }

        .theme-toggle:hover {
            transform: scale(1.1);
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .hero-section {
                padding: 2rem 1rem;
            }

            .features-section, .showcase-section, .tech-section {
                padding: 3rem 1rem;
            }

            .showcase-image {
                height: 250px;
                margin-bottom: 2rem;
            }

            .stat-number {
                font-size: 2rem;
            }
        }
    </style>
</head>
<body>
    <!-- Theme Toggle -->
    <button class="theme-toggle" onclick="toggleTheme()" aria-label="Changer le thème">
        <i class="bi bi-moon-fill" id="theme-icon"></i>
    </button>

    <!-- Hero Section -->
    <section class="hero-section">
        <div class="container">
            <div class="hero-content mx-auto">
                <h1 class="hero-title">Centre Informatique UGANC</h1>
                <p class="hero-subtitle">Plateforme moderne de gestion du parc informatique pour l'excellence technologique et académique</p>
                <button class="btn btn-start" onclick="window.location.href='{{ path('app_login') }}'">
                    Accéder à la plateforme
                    <i class="bi bi-arrow-right ms-2"></i>
                </button>
            </div>
        </div>
    </section>

    <!-- Stats Section -->
    <section class="stats-section">
        <div class="container">
            <div class="row">
                <div class="col-md-3 col-6">
                    <div class="stat-card">
                        <span class="stat-number">500+</span>
                        <span class="stat-label">Équipements</span>
                    </div>
                </div>
                <div class="col-md-3 col-6">
                    <div class="stat-card">
                        <span class="stat-number">24/7</span>
                        <span class="stat-label">Surveillance</span>
                    </div>
                </div>
                <div class="col-md-3 col-6">
                    <div class="stat-card">
                        <span class="stat-number">99.9%</span>
                        <span class="stat-label">Disponibilité</span>
                    </div>
                </div>
                <div class="col-md-3 col-6">
                    <div class="stat-card">
                        <span class="stat-number">15+</span>
                        <span class="stat-label">Départements</span>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Features Section -->
    <section class="features-section">
        <div class="container">
            <h2 class="section-title">Gestion Intelligente du Parc Informatique</h2>
            <p class="section-subtitle">
                Notre plateforme offre une solution complète pour administrer, surveiller et optimiser 
                l'ensemble des ressources informatiques de l'université.
            </p>
            
            <div class="row">
                <div class="col-lg-4 col-md-6">
                    <div class="feature-card animate-on-scroll">
                        <i class="bi bi-hdd-stack feature-icon"></i>
                        <h3 class="feature-title">Inventaire Intelligent</h3>
                        <p class="feature-description">
                            Suivi automatisé de tous les équipements avec détection des changements, 
                            historique complet et alertes de maintenance préventive.
                        </p>
                    </div>
                </div>
                <div class="col-lg-4 col-md-6">
                    <div class="feature-card animate-on-scroll">
                        <i class="bi bi-wrench-adjustable feature-icon"></i>
                        <h3 class="feature-title">Support Technique</h3>
                        <p class="feature-description">
                            Système de tickets avancé avec priorisation automatique, 
                            suivi en temps réel et escalade intelligente des incidents.
                        </p>
                    </div>
                </div>
                <div class="col-lg-4 col-md-6">
                    <div class="feature-card animate-on-scroll">
                        <i class="bi bi-diagram-3 feature-icon"></i>
                        <h3 class="feature-title">Cartographie Réseau</h3>
                        <p class="feature-description">
                            Visualisation interactive de l'infrastructure réseau avec 
                            monitoring en temps réel et détection proactive des anomalies.
                        </p>
                    </div>
                </div>
                <div class="col-lg-4 col-md-6">
                    <div class="feature-card animate-on-scroll">
                        <i class="bi bi-shield-check feature-icon"></i>
                        <h3 class="feature-title">Sécurité Avancée</h3>
                        <p class="feature-description">
                            Authentification multi-facteurs, chiffrement bout-en-bout 
                            et audit complet des accès pour une sécurité maximale.
                        </p>
                    </div>
                </div>
                <div class="col-lg-4 col-md-6">
                    <div class="feature-card animate-on-scroll">
                        <i class="bi bi-graph-up feature-icon"></i>
                        <h3 class="feature-title">Analytics Prédictifs</h3>
                        <p class="feature-description">
                            Tableaux de bord interactifs avec intelligence artificielle 
                            pour anticiper les besoins et optimiser les performances.
                        </p>
                    </div>
                </div>
                <div class="col-lg-4 col-md-6">
                    <div class="feature-card animate-on-scroll">
                        <i class="bi bi-cloud-check feature-icon"></i>
                        <h3 class="feature-title">Cloud Hybride</h3>
                        <p class="feature-description">
                            Intégration transparente entre infrastructure locale et cloud 
                            pour une flexibilité et une évolutivité optimales.
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Showcase Section -->
    <section class="showcase-section">
        <div class="container">
            <div class="row align-items-center showcase-item">
                <div class="col-lg-6 animate-on-scroll">
                    <img src="https://images.unsplash.com/photo-1558494949-ef010cbdcc31?ixlib=rb-4.0.3&auto=format&fit=crop&w=1000&q=80" 
                         alt="Centre de données moderne avec serveurs" class="showcase-image">
                </div>
                <div class="col-lg-6 animate-on-scroll">
                    <div class="showcase-content">
                        <h3>Infrastructure de Pointe</h3>
                        <p>
                            Notre centre de données est équipé des dernières technologies : 
                            serveurs haute performance, systèmes de refroidissement intelligents, 
                            et redondance complète pour garantir une disponibilité maximale 
                            des services informatiques de l'université.
                        </p>
                    </div>
                </div>
            </div>
            
            <div class="row align-items-center showcase-item">
                <div class="col-lg-6 order-lg-2 animate-on-scroll">
                    <img src="https://images.unsplash.com/photo-1551288049-bebda4e38f71?ixlib=rb-4.0.3&auto=format&fit=crop&w=1000&q=80" 
                         alt="Tableau de bord analytics avec graphiques" class="showcase-image">
                </div>
                <div class="col-lg-6 order-lg-1 animate-on-scroll">
                    <div class="showcase-content">
                        <h3>Interface Utilisateur Intuitive</h3>
                        <p>
                            Dashboard moderne avec visualisations interactives, 
                            navigation fluide et personnalisation avancée. 
                            Accédez rapidement aux informations essentielles 
                            grâce à une interface pensée pour l'efficacité.
                        </p>
                    </div>
                </div>
            </div>
            
            <div class="row align-items-center showcase-item">
                <div class="col-lg-6 animate-on-scroll">
                    <img src="https://images.unsplash.com/photo-1563013544-824ae1b704d3?ixlib=rb-4.0.3&auto=format&fit=crop&w=1000&q=80" 
                         alt="Équipe technique travaillant sur ordinateurs" class="showcase-image">
                </div>
                <div class="col-lg-6 animate-on-scroll">
                    <div class="showcase-content">
                        <h3>Support Technique Expert</h3>
                        <p>
                            Une équipe dédiée de techniciens qualifiés assure 
                            la maintenance, le support et l'évolution continue 
                            du système. Formation continue et veille technologique 
                            pour rester à la pointe de l'innovation.
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Technology Section -->
    <section class="tech-section">
        <div class="container">
            <h2 class="section-title animate-on-scroll">Technologies Employées</h2>
            <p class="section-subtitle animate-on-scroll">
                Stack technologique moderne pour une performance et une fiabilité optimales
            </p>
            
            <div class="tech-grid">
                <div class="tech-item animate-on-scroll">
                    <i class="bi bi-server tech-icon"></i>
                    <div class="tech-name">Serveurs Linux</div>
                </div>
                <div class="tech-item animate-on-scroll">
                    <i class="bi bi-database tech-icon"></i>
                    <div class="tech-name">Bases de Données</div>
                </div>
                <div class="tech-item animate-on-scroll">
                    <i class="bi bi-wifi tech-icon"></i>
                    <div class="tech-name">Réseau Gigabit</div>
                </div>
                <div class="tech-item animate-on-scroll">
                    <i class="bi bi-shield-lock tech-icon"></i>
                    <div class="tech-name">Cybersécurité</div>
                </div>
                <div class="tech-item animate-on-scroll">
                    <i class="bi bi-cloud tech-icon"></i>
                    <div class="tech-name">Cloud Computing</div>
                </div>
                <div class="tech-item animate-on-scroll">
                    <i class="bi bi-phone tech-icon"></i>
                    <div class="tech-name">Mobile First</div>
                </div>
            </div>
        </div>
    </section>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
    <script>
        // Animation au scroll
        document.addEventListener('DOMContentLoaded', function () {
            const elements = document.querySelectorAll('.animate-on-scroll');
            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.classList.add('visible');
                        observer.unobserve(entry.target);
                    }
                });
            }, { threshold: 0.1 });

            elements.forEach(element => observer.observe(element));
        });

        // Theme Toggle
        function toggleTheme() {
            const html = document.documentElement;
            const themeIcon = document.getElementById('theme-icon');
            const currentTheme = html.getAttribute('data-theme');
            
            if (currentTheme === 'dark') {
                html.setAttribute('data-theme', 'light');
                themeIcon.className = 'bi bi-moon-fill';
            } else {
                html.setAttribute('data-theme', 'dark');
                themeIcon.className = 'bi bi-sun-fill';
            }
        }

        // Smooth scrolling pour les ancres
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });

        // Parallax effect pour le hero
        window.addEventListener('scroll', () => {
            const scrolled = window.pageYOffset;
            const hero = document.querySelector('.hero-section');
            if (hero) {
                hero.style.transform = `translateY(${scrolled * 0.5}px)`;
            }
        });
    </script>
</body>
</html>