{# templates/equipement/edit.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Modifier un Équipement{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="mb-4 text-center text-primary">Modifier l'équipement</h1>

    {% include 'equipement/_form.html.twig' with {'button_label': 'Mettre à jour'} %}

    <div class="mt-3 text-center">
        <a href="{{ path('app_equipement_index') }}" class="btn btn-secondary">Retour à la liste</a>
    </div>

    <div class="mt-2 text-center">
        {% include 'equipement/_delete_form.html.twig' %}
    </div>
</div>
{% endblock %}
