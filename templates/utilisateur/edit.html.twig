{# templates/utilisateur/edit.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Modifier Utilisateur{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="text-center mb-4">Modifier Utilisateur</h1>

        {% include 'utilisateur/_form.html.twig' with {'button_label': 'Mettre à jour'} %}

        <div class="mt-3 text-center">
            <a href="{{ path('app_utilisateur_index') }}" class="btn btn-secondary">Retour à la liste</a>
        </div>
    </div>
{% endblock %}
